import { extendTheme } from "@chakra-ui/react";

export default extendTheme({
  colors: {
    primary: "#0F0019",
  },
  styles: {
    global: {
      // styles for the `body`
      body: {},
      // styles for the `a`
      a: {},
    },
  },
});
