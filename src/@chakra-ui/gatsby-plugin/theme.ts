import { extendTheme } from "@chakra-ui/react";

export default extendTheme({
  colors: {
    primary: "rebeccapurple",
  },
  styles: {
    global: {
      // styles for the `body`
      body: {},
      // styles for the `a`
      a: {},
    },
  },
});
